<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
* 백화점 옷 조회 Mybatis 매퍼
* @author 임재성
* @since 2024.08.23
* @version 1.0
*
* <pre>
* 수정일        	수정자        수정내용
* ==========  =========    =========
* 2024.08.23   임재성         최초 생성

*
* </pre>
-->
<mapper namespace="com.themore.moamoatown.clothes.mapper.ClothesMapper">
    <select id="getClothesListWithPaging" resultType="ClothesResponseDTO">
         <![CDATA[
            select clothes_id,brand,name,price,type,img_url
            from clothes
            ORDER BY clothes_id
            OFFSET #{offset} ROWS FETCH NEXT #{size} ROWS ONLY

        ]]>
    </select>

    <insert id="insertIntoCloset">
    <![CDATA[
        INSERT INTO CLOSET (CLOSET_ID, CLOTHES_ID, MEMBER_ID)
        VALUES (CLOSET_SEQ.NEXTVAL, #{clothesId}, #{memberId})

    ]]>
    </insert>


    <select id="findWishItemsByTownId" resultType="com.themore.moamoatown.clothes.dto.WishItemResponseDTO">
        <![CDATA[
        SELECT wish_id AS wishId, town_id AS townId, name, price
        FROM wish
        WHERE town_id = #{townId}
        ]]>
    </select>
</mapper>